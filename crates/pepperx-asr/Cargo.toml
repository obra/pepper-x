[package]
name = "pepperx-asr"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
hound = "3.5.1"
parakeet-rs = { version = "0.3.4", default-features = false, features = ["cpu", "ort-defaults"] }
